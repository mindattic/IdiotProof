{
  "name": "idiotproof-cypress",
  "version": "1.0.0",
  "private": true,
  "description": "End-to-end UI tests for IdiotProof's Blazor Server app. Run after `dotnet run --project IdiotProof.Blazor` so the dev server is up at https://localhost:5001 (or override CYPRESS_BASE_URL).",
  "scripts": {
    "open": "cypress open",
    "run": "cypress run",
    "ci": "cypress run --browser chrome --headless"
  },
  "devDependencies": {
    "axe-core": "^4.10.2",
    "cypress": "^13.15.0",
    "cypress-axe": "^1.5.0",
    "typescript": "^5.9.3"
  }
}
