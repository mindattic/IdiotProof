{
  "name": "idiotproof-landing",
  "private": true,
  "version": "1.0.0",
  "description": "README -> index.htm renderer for the IdiotProof mindattic.com landing page. Not the .NET app itself.",
  "scripts": {
    "build": "node scripts/cli/build-html.js",
    "deploy": "powershell -NoProfile -ExecutionPolicy Bypass -File scripts/cli/deploy.ps1"
  },
  "dependencies": {
    "highlight.js": "^11.9.0",
    "marked": "^4.3.0"
  }
}
